; Create React App pins TypeScript 4.x, which trips better-auth's optional
; Prisma peer dependency. better-auth's React client doesn't use Prisma, so we
; relax peer resolution for installs in this (frontend) package.
legacy-peer-deps=true
